include common/head

.bnrMainCont.bnrBgOlay
  .bnrBg(style="background-image: url('/images/banner-featshop.jpg');")
.inrBredCrumbCont
  .centerAlign
    ul.bredCrumbWrp
      li
        a(href='/') Home
      li
        a(href='javascript:void(0)',id="breadcr")
section.secPad.pt0
  .centerAlign
    ul.featShopMainCont.popularGridWrap
     - for (var x = 0; x < 12; x++)
        li
          a.sclHov(href='/brand/#{stores[x].URL}')
            img(src=global.config.variable.apiPath+'/#{stores[x].storeBanner}', alt='img')
          .featShpInfWrp
            a.popGrdTtle(href='/brand/#{stores[x].URL}') #{stores[x].storeName}
            .popGrdDesc #{stores[x].storeDiscription}
            // .featShpActBtn
            //   a.act_icn.icn_bokMark(href='#')
            //     i
            //   a.act_icn.icn_sharePst(href='#')
            //     i
          .featShpRate #{stores[x].avgRating}
    .txtCent.mt60
      a.defBtn.loadMore(id="loadmoreshop") Load More

include common/footer
script.
  $(document).ready(function() {
    // window.addEventListener('scroll', throttle(populate, 1000));
    // var scrolled = false;
    // function throttle(fn, wait) {
    //     var time = Date.now();        
    //     return function() {
    //         if(!scrolled){
    //             scrolled = true;
    //           if ((time + wait - Date.now()) < 0) {
    //               fn();
    //               time = Date.now();
    //           }
    //             scrolled = false;
    //         };
    //     }
    // }
    // function callback() {
    //   $("#loadmoreshop").trigger("click");
    // }
    $("#breadcr").append(localStorage.getItem('bread'));
  });